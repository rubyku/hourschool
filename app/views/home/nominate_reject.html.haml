

.modal
  .welcome-header Can't make it...

  = form_tag nominate_reject_send_path, :method => 'post' do
    = hidden_field_tag 'reqid', @req.id
    .form-question= "Can you please write a short note to tell us why you can't teach the #{@course.title} class?"
    = text_area_tag 'message', '', :class => 'form-tall'
    .page-section-float-left
      = submit_tag "Send message", :class => "orange button"
