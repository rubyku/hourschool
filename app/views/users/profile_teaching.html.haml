.page
  .page-content

    .page-section-float-left
      %ul.sub-nav
        %li.sub-nav-inactive
          %a.sub-nav-inactive(href="/profile") Classes attending
        %li.sub-nav-active
          %a.sub-nav-active(href="/profile_teaching") Classes teaching  
        %li.sub-nav-inactive
          %a.sub-nav-inactive(href="/profile-suggest") Suggestions
        - if current_user.is_admin?
          %li.sub-nav-inactive 
            %a.sub-nav-inactive(href="/profile-pending") Admin


    .page-section-float-left  
      - if @approved_courses.any? || @pending_courses.count != 0 || !current_user.photo.exists? || current_user.zip.blank? || current_user.location.blank?
        .notification-center
          #notification-header Notifications 
          - if @approved_courses.any?
            %img{:src => '/images/v2/notification_bolt.png'}
            = "Your class(s) has been approved!" 
            %a.notification(href="/profile-approved") Fill out the class profile
            = "to post it live!"
            %br
            %br
          - if @pending_courses.count != 0
            %img{:src => '/images/v2/notification_bolt.png'}
            = "Your proposal(s) has been submitted successfully. We'll get back to you very soon. In the meantime,"
            %a.notification(href="/pdf/handbook-111311.pdf") check out our teacher's handbook.
            %br
            %br
          - if !current_user.photo.exists?
            %img{:src => '/images/v2/notification_bolt.png'}
            = "You don't have a profile photo." 
            %a.notification(href="/users/edit") Upload one now!
          - if current_user.zip.blank? || current_user.location.blank?
            %img{:src => '/images/v2/notification_bolt.png'}
            = "Want to get notified when new classes happen in your city?" 
            %a.notification(href="/users/edit") Tell us where you live!
      - else
        /do nothing


    .page-section-float-left
      .welcome-header Upcoming classes teaching
      %img.greyheader_half{:src => '/images/v2/greyheader_half.png'}
      .classRow
        = render :partial => "classes", :locals => {:classes_this_week => current_user.recent_classes_as_teacher}
        = render :partial => "classes_approved", :locals => {:pending => current_user.approved_classes}


    .page-section-float-right
      %table.view-more
        %tr
          %td
            %a.view-more(href="/profile_past_taught") Classes I taught in the past
          %td
            %img.view-more{:src => '/images/v2/arrow_grey_pointright.png'}
            


= render :partial => 'sidebar'