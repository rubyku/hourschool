:css
  a{color: #ff7733;}

.page
  .page-content
    .page-section-float-left
      .class-logistic-block
        - if @course.photo.present?
          = image_tag(@course.photo.url(:small), :size => "190x120")
          %img.class-arrow{:src => '/images/v2/arrow_white_pointup.png'}
        - else
          - if !@course.categories.first.nil?
            = image_tag("#{DEF_IMAGES[@course.categories.first.name]}",:size => "190x120")
          - else
            = image_tag("/images/v2-courses/DefaultClassPics_Generic.png",:size => "190x120")
          %img.class-arrow{:src => '/images/v2/arrow_white_pointup.png'}
      .class-description-block
        .class-title-big
          = "#{@course.title}"
        .happening-message-header
          = "#{@course.city.try(:name)}, #{@course.city.try(:state)}"
        %br
        .heading10-class Classes are always more fun with a group of friends. Share this class!
        = render :partial => "add_this_small"

    .page-section-float-left
      .class-section-heading
        = "This class will cover"
      %p= @course.description

    .comment-section
      .class-section-heading
        = "Join the conversation"
      = render :partial => "comments", :locals => {:course => @course}


= content_for :head do
  :erb
    <meta property="og:description" content="<%= @course.description %>"        />
    <meta property="og:image"       content="<%= @course.photo.url(:small) %>"  />
    <meta property="og:title"       content="<%= @course.title %>"              />
    <meta property="og:url"         content="<%= course_url(@course) %>"        />


= content_for :sidebar do
  = render :partial => "sidebar", :locals => {:course => @course}

