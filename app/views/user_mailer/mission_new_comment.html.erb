<p>Hello <%= @user.name.split(" ").first %>,</p>

<p><%= link_to @new_comment.user.name, user_url(@new_comment.user) %> left a comment in your mission <%= link_to "#{@mission.verb} #{@mission.title}", mission_url(@mission) %>.
</p>

<p>"<%= @new_comment.body %>"</p>

<p><%= link_to "Reply", comment_url(@new_comment) %> to <%= @new_comment.user.name.split(" ").first %></p>