<%= @user.name %> just submitted a feedback form for the <%= @course.title %> class.

<% if @course.teacher ==  @user %>
  <% @students.each do |student_hash| %>
    <%= ::User.find(student_hash[0]).name %> wants to teach <%=  student_hash[1]["want_to_teach"] %> and wants to learn <%= student_hash[1]["want_to_learn"] %>
  <% end %>
<% end %>

Other feedback: 
<%= @general_feedback %>


