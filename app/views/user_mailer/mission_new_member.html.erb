<p<Hello <%= @user.name.split(" ").first %>,</p>

<p><%= link_to "#{@new_member.name}", user_url(@new_member) %> just joined your mission <%= link_to "#{@mission.verb} #{@mission.title}", mission_url(@mission) %>.</p>

<p>Say hi!</p>
