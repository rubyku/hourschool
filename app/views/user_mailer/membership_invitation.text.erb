<% if @existing_user %>
Check it out!
<%= home_url(:subdomain => @account.subdomain) %>
<% else %>
Create a password and signup here:
<%= edit_password_url(@user, :reset_password_token => @user.reset_password_token, :subdomain => @account.subdomain) %>
<% end %>