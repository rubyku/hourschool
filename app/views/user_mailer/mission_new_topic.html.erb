<p>Hello <%= @user.name.split(" ").first %>,</p>

<p><%= link_to @new_topic.user.name, user_url(@new_topic.user) %> added a new topic in your mission <%= link_to "#{@mission.verb} #{@mission.title}", mission_url(@mission) %>.</p>

<p>"<%= @new_topic.title %>"</p>

<p><%= link_to "Organize an event", mission_topic_url(@mission, @new_topic) %> for this topic</p>