var learn_temp = '#vote_count_' + "<%= "#{@csuggestion.id}" %>"
var learn_result = "<%= @csuggestion.votes_for.to_s %>"
$(learn_temp).html(learn_result)

var learn_temp = '#vote_link_' + "<%= "#{@csuggestion.id}" %>"
var learn_result = "You voted"
$(learn_temp).html(learn_result)
